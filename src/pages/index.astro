---
import Layout from "../layouts/Layout.astro";
import HeroSection from "../components/HeroSection.astro";
import About from "../components/abou.jsx";
import Projekts from "../components/projekts.astro";

import Reveal from "../components/Reveal.jsx";
import Contact from "../components/Contact.jsx";
// Swiper-Importe entfernt

// Beispielprojektdaten
const projects = [
  {
    title: "🌿 Kraut & Knolle – Fiktive Restaurant-Website",
    description:
      "Eine moderne, fiktive Restaurant-Website mit klarem Design, ansprechender Speisekarte und Fokus auf Benutzerfreundlichkeit.",
    languages: ["Astro", "React", "CSS", "tailwind"],
    imageUrl: "./bilder/kraut und knolle.png",
    detailsUrl: "/projekte/kraut-und-knolle",
  },
];

const formspreeEndpoint = "https://formspree.io/f/xblzjorp";
---

<Layout>
  <main class="bg-background text-textColor min-h-screen">
    <div id="home" class="scroll-mt-28">
      <Reveal variant="fade-in" client:visible>
        <HeroSection />
      </Reveal>
    </div>

    <div id="about" class="scroll-mt-28">
      <Reveal variant="slide-right" client:visible>
        <About />
      </Reveal>
    </div>

    <section id="projects" class="py-16 scroll-mt-28">
      <div class="container mx-auto px-4">
        <Reveal variant="fade-up" client:visible>
          <h2 class="text-4xl font-bold text-center mb-12">Meine Projekte</h2>
        </Reveal>
        <Reveal variant="zoom" delay={0.05} client:visible>
          <Projekts projects={projects} />
        </Reveal>
      </div>
    </section>

    <div id="contact" class="scroll-mt-28">
      <Reveal variant="slide-left" client:visible>
        <Contact client:visible />
      </Reveal>
    </div>
  </main>
</Layout>

<style>
  .container {
    max-width: 1200px;
  }
</style>
